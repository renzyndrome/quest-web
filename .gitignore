# build output
dist/
.astro/

# raw asset drops from the church admin (huge originals; the optimized
# derivatives under cms/seed/assets/ are what gets committed)
public/images/*.zip
public/images/WEBSITE*/

# dependencies
node_modules/

# environment — never commit secrets
.env
.env.*
!.env.example
cms/.env

# e2e (Playwright)
test-results/
playwright-report/
playwright/.cache/
# build produced by the CMS integration suite (kept apart from dist/)
dist-cms/

# logs
npm-debug.log*

# os
.DS_Store

# raw photo drops from the church (converted copies live in public/images/site/)
public/images/to be compressed*/
