# next / build output
.next/
next-env.d.ts
node_modules/

# generated by `next dev` (agent guidance files, re-created on each run)
AGENTS.md
CLAUDE.md

# payload generated types (regenerate with `payload generate:types`)
payload-types.ts

# e2e artifacts (sqlite db + uploaded media for the integration run)
e2e.db
e2e.db-journal
media-e2e/

# uploads (local dev); production uses a Docker volume
media/

# environment — never commit secrets
.env
.env.*
!.env.example
